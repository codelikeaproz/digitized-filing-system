# Environment files are split by service:
#   backend/.env.example  -> copy to backend/.env (Django variables)
#   frontend/.env.example -> copy to frontend/.env (Vite variables)
#
# Do not use a shared root-level .env file.
